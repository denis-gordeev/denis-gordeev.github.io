---
published: false
---

# TODO

Latest automation round: 2026-04-04

## Completed in this round

- Read `AUTOWORK_INSTRUCTIONS.md` and avoided creating another `AUTOWORK` post because one was already added on 2026-04-04.
- Tried to follow the issue and PR step, but GitHub GraphQL access is still unavailable locally because `gh` is not authenticated.
- Replaced the default Minima home feed layout with a more intentional landing page that highlights the profile, archive, CV, and recent posts.
- Added lightweight custom styling for the landing page while keeping the Jekyll/Minima setup simple.
- Declared `About` and `Archive` explicitly in `header_pages` so the top navigation stays predictable.

## Next actions

- Refresh the CV file if a newer public version is available.
- Keep adding AUTOWORK posts only when there is a meaningful public update and not more than once per day.
- Add a lightweight projects or talks section if there is public material worth linking from the About page.
- Consider adding a small visual preview or excerpt to the archive or recent-post blocks if the site needs more scanning context.
- Authenticate `gh` locally so future rounds can inspect open GitHub issues and PRs instead of skipping that step.
